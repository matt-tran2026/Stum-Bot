{
  "name": "sports-bet-mvp",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm --workspace @mvp/api run dev",
    "dev:bot": "npm --workspace @mvp/bot run dev",
    "dev": "echo Start each app with dev:api and dev:bot",
    "build": "npm run -ws build"
  }
}
